[mysqld]
server-id=101
bind-address=0.0.0.0
log_bin=mysql-bin
binlog_format=ROW
gtid_mode=ON
enforce_gtid_consistency=ON
log_slave_updates=ON

# Optional tuning
sync_binlog=1
innodb_flush_log_at_trx_commit=1
